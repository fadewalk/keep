{/* This snippet is automatically generated using scripts/docs_render_provider_snippets.py
Do not edit it manually, as it will be overwritten */}

## 认证
此提供商需要身份验证。
- **host_url**: Grafana Loki 主机 URL（必需：True，敏感：False）
- **verify**: 启用 SSL 验证（必需：False，敏感：False）
- **authentication_type**: 身份验证类型（必需：True，敏感：False）
- **username**: HTTP 基本身份验证 - 用户名（必需：False，敏感：False）
- **password**: HTTP 基本身份验证 - 密码（必需：False，敏感：True）
- **x_scope_orgid**: X-Scope-OrgID 头身份验证（必需：False，敏感：False）

某些范围可能需要才能通过提供商执行特定操作或查询。以下是相关范围及其用例的摘要：
- **authenticated**: 实例有效且用户已身份验证


## 在工作流中

此提供商可用于工作流。


作为"步骤"来查询数据，示例：
```yaml
steps:
    - name: Query grafana_loki
      provider: grafana_loki
      config: "{{ provider.my_provider_name }}"
      with:
        query: {value}
        limit: {value}
        time: {value}
        direction: {value}
        start: {value}
        end: {value}
        since: {value}
        step: {value}
        interval: {value}
        queryType: {value}
```





请查看以下工作流示例：
- [query_grafana_loki.yaml](https://github.com/keephq/keep/blob/main/examples/workflows/query_grafana_loki.yaml)
