---
title: "Victoriametrics Provider"
sidebarTitle: "Victoriametrics Provider"
description: "The VictoriametricsProvider allows you to fetch alerts in Victoriametrics."
---
import AutoGeneratedSnippet from '/snippets/providers/victoriametrics-snippet-autogenerated.mdx';


<AutoGeneratedSnippet />

## 连接 Provider

1. 确保您有一个可访问的主机和端口上运行的 VMAlert 实例。
2. 在初始化 provider 时，在 Victoriametrics provider 配置中包含主机和端口信息。

## 查询 Victoriametrics

Victoriametrics provider 允许您通过 `query` 和 `query_range` 类型从 Victoriametrics 查询。以下是可用于查询的参数：

1. `query` 类型：

   - `query`：在 Victoriametrics 上执行的查询。示例：`sum(rate(http_requests_total{job="api-server"}[5m]))`。
   - `start`：查询数据的时间。示例：`2024-01-01T00:00:00Z`

2. `query_range` 类型：
   - `query`：在 Victoriametrics 上执行的查询。示例：`sum(rate(http_requests_total{job="api-server"}[5m]))`。
   - `start`：查询数据的开始时间。示例：`2024-01-01T00:00:00Z`
   - `end`：查询数据的结束时间。示例：`2024-01-01T00:00:00Z`
   - `step`：查询使用的步长。示例：`15s`

## 相关链接

- [Victoriametrics](https://victoriametrics.com/docs/)
- [VMAlert](https://victoriametrics.github.io/vmalert.html)
