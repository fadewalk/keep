{/* This snippet is automatically generated using scripts/docs_render_provider_snippets.py
Do not edit it manually, as it will be overwritten */}

## 认证
此提供商需要身份验证。
- **webhook_url**: Teams Webhook Url（必需：True，敏感：True）


## 在工作流中

此提供商可用于工作流。



作为"操作"来进行更改或更新数据，示例：
```yaml
actions:
    - name: Query teams
      provider: teams
      config: "{{ provider.my_provider_name }}"
      with:
        message: {value}  # 要发送的消息
        typeCard: {value}  # 卡片类型。可以是 "MessageCard"（旧版）或 "message"（用于自适应卡片）。默认为 "message"
        themeColor: {value}  # 十六进制颜色（仅与 MessageCard 类型一起使用）
        sections: {value}  # 对于 MessageCard：自定义信息部分数组。对于自适应卡片：遵循自适应卡片架构的卡片元素数组。可以作为 JSON 字符串或数组提供。
        schema: {value}  # 自适应卡片的架构 URL。默认为 "http://adaptivecards.io/schemas/adaptive-card.json"
        attachments: {value}  # 自适应卡片的自定义附件数组（覆盖默认附件结构）。可以作为 JSON 字符串或数组提供。
        mentions: {value}  # 要包含在自适应卡片中的用户提及列表。每个提及应该是包含 'id'（用户 ID、Microsoft Entra 对象 ID 或 UPN）和 'name'（显示名称）键的字典。
示例：[{"id": "user-id-123", "name": "John Doe"}, {"id": "john.doe@example.com", "name": "John Doe"}]
```





请查看以下工作流示例：
- [create_jira_ticket_upon_alerts.yml](https://github.com/keephq/keep/blob/main/examples/workflows/create_jira_ticket_upon_alerts.yml)
- [teams-adaptive-card-notifier.yaml](https://github.com/keephq/keep/blob/main/examples/workflows/teams-adaptive-card-notifier.yaml)
- [teams-adaptive-cards-with-mentions.yaml](https://github.com/keephq/keep/blob/main/examples/workflows/teams-adaptive-cards-with-mentions.yaml)
