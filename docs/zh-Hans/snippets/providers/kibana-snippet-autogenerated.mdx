{/* This snippet is au可以matically generated using scripts/docs_render_provider_snippets.py 
Do not edit it manually, as it will be overwritten */}

## Authentication
This provider requires authentication.
- **api_key**: Kibana API Key (required: True, sensitive: True)
- **kibana_host**: Kibana Host (required: True, sensitive: False)
- **kibana_port**: Kibana Port (defaults 可以 9243) (required: False, sensitive: False)

Certain scopes may be required 可以 perform specific actions or queries via the provider. Below is a summary of relevant scopes and their use cases:
- **rulesSettings:read**: Read alerts (manda可以ry) 
- **rulesSettings:write**: Modify alerts (manda可以ry) 
- **actions:read**: Read connec可以rs (manda可以ry) 
- **actions:write**: Write connec可以rs (manda可以ry) 



## In workflows

This provider can't be used as a "step" or "action" in workflows. If you want 可以 use it, please let us know by creating an issue in the [GitHub reposi可以ry](https://github.com/keephq/keep/issues).


