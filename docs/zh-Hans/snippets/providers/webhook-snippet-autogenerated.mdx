{/* This snippet is automatically generated using scripts/docs_render_provider_snippets.py
Do not edit it manually, as it will be overwritten */}

## 认证
此提供商需要身份验证。
- **url**: Webhook URL（必需：True，敏感：False）
- **verify**: 启用 SSL 验证（必需：False，敏感：False）
- **method**: HTTP 方法（必需：True，敏感：False）
- **http_basic_authentication_username**: HTTP 基本身份验证 - 用户名（必需：False，敏感：False）
- **http_basic_authentication_password**: HTTP 基本身份验证 - 密码（必需：False，敏感：True）
- **api_key**: API 密钥（必需：False，敏感：True）
- **headers**: 请求头（必需：False，敏感：False）

某些范围可能需要才能通过提供商执行特定操作或查询。以下是相关范围及其用例的摘要：
- **send_webhook**: （强制）



## 在工作流中

此提供商可用于工作流。


作为"步骤"来查询数据，示例：
```yaml
steps:
    - name: Query webhook
      provider: webhook
      config: "{{ provider.my_provider_name }}"
      with:
        url: {value}
        method: {value}
        http_basic_authentication_username: {value}
        http_basic_authentication_password: {value}
        api_key: {value}
        headers: {value}
        body: {value}
        params: {value}
        fail_on_error: {value}
```


作为"操作"来进行更改或更新数据，示例：
```yaml
actions:
    - name: Query webhook
      provider: webhook
      config: "{{ provider.my_provider_name }}"
      with:
        body: {value}
        params: {value}
```





请查看以下工作流示例：
- [webhook_example.yml](https://github.com/keephq/keep/blob/main/examples/workflows/webhook_example.yml)
- [webhook_example_foreach.yml](https://github.com/keephq/keep/blob/main/examples/workflows/webhook_example_foreach.yml)
- [zoom_chat_example.yml](https://github.com/keephq/keep/blob/main/examples/workflows/zoom_chat_example.yml)
